{
  "name": "SD-GNN: MUTAG SS GCN (attention pooling)",
  "dataset_name": "MUTAG",
  "model_name": "SD-GNN",
  "task": "Binary-Classification",
  "model_config": {
    "mpnn_type": "gcn",
    "node_feature_dim": 7,
    "hidden_dim": 64,
    "mpnn_layers": 4,
    "dropout": 0.1,
    "out_dim": 2,
    "pooling": "mean",
    "temperature": 0.5,
    "subgraph_sampling": true,
    "subgraph_param": {
      "k": 6,
      "m": 6,
      "pooling": "sum"
    },
    "kwargs": {
      "aggregator": "weighted_mean",
      "sub_num_layers": 3,
      "val_ema_alpha": 0.85
    }
  },
  "train": {
    "epochs": 50,
    "train_batch_size": 64,
    "val_batch_size": 64,
    "lr": 0.001,
    "weight_decay": 1e-05,
    "optimizer": "adam",
    "scheduler": {
      "type": "reduce_on_plateau",
      "patience": 20
    },
    "loss_fn": "CrossEntropyLoss",
    "metric": "ACC",
    "grad_clip": 1.0
  },
  "device": "cuda",
  "seed": 42,
  "num_workers": 4,
  "log_dir": "logs",
  "checkpoint_dir": "checkpoints",
  "save_every": 1,
  "keep_last_k": 3,
  "use_amp": false,
  "grad_clip": 2.0,
  "sampler": "uniform"
}
